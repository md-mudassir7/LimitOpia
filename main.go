package main

import (
	"log"
)

func main() {
	// cfg := config.Load()

	// mux := http.NewServeMux()

	// mux.Handle("/example", middleware.RateLimiterMiddleware(http.HandlerFunc(handlers.ExampleHandler)))

	log.Println("Starting server on:")
	// if err := http.ListenAndServe(cfg.ServerAddress, mux); err != nil {
	// 	logger.Error("Server failed to start: %v", err)
	// }
}
